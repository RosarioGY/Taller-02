<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender with structured logging -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp/>
                <logLevel/>
                <loggerName/>
                <mdc/>
                <arguments/>
                <message/>
                <stackTrace/>
            </providers>
        </encoder>
    </appender>

    <!-- File appender for application logs -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/loan-validation.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/loan-validation.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp/>
                <logLevel/>
                <loggerName/>
                <mdc/>
                <arguments/>
                <message/>
                <stackTrace/>
            </providers>
        </encoder>
    </appender>

    <!-- Error file appender for error logs only -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/loan-validation-errors.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/loan-validation-errors.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>90</maxHistory>
            <totalSizeCap>5GB</totalSizeCap>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp/>
                <logLevel/>
                <loggerName/>
                <mdc/>
                <arguments/>
                <message/>
                <stackTrace/>
            </providers>
        </encoder>
    </appender>

    <!-- Development profile - console only -->
    <springProfile name="dev">
        <appender name="CONSOLE_DEV" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        
        <root level="DEBUG">
            <appender-ref ref="CONSOLE_DEV"/>
        </root>
        
        <logger name="com.techgirls.loanvalidation" level="DEBUG"/>
        <logger name="org.springframework.web" level="DEBUG"/>
        <logger name="reactor.netty" level="INFO"/>
    </springProfile>

    <!-- Test profile - reduced logging -->
    <springProfile name="test">
        <appender name="CONSOLE_TEST" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss} %-5level %logger{25} - %msg%n</pattern>
            </encoder>
        </appender>
        
        <root level="WARN">
            <appender-ref ref="CONSOLE_TEST"/>
        </root>
        
        <logger name="com.techgirls.loanvalidation" level="INFO"/>
    </springProfile>

    <!-- Production profile - structured JSON logging -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
            <appender-ref ref="ERROR_FILE"/>
        </root>
        
        <logger name="com.techgirls.loanvalidation" level="INFO"/>
        <logger name="com.techgirls.loanvalidation.web.GlobalExceptionHandler" level="WARN"/>
        <logger name="com.techgirls.loanvalidation.service" level="INFO"/>
        <logger name="org.springframework" level="WARN"/>
        <logger name="reactor" level="WARN"/>
        <logger name="io.netty" level="WARN"/>
    </springProfile>

    <!-- Default configuration -->
    <springProfile name="!dev,!test,!prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
        
        <logger name="com.techgirls.loanvalidation" level="INFO"/>
        <logger name="org.springframework" level="INFO"/>
    </springProfile>

    <!-- MDC keys for structured logging -->
    <!-- Available MDC keys:
         - traceId: Request correlation ID
         - errorType: Type of error for categorization
         - userId: User identifier (when available)
         - requestId: HTTP request identifier
         - operation: Business operation being performed
    -->
    
</configuration>